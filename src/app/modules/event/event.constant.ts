const FilterableFields = [
  'search',
  'is_featured',
  'is_public',
  'is_paid',
  'is_virtual',
  'status',
];

const SearchableFields = ['title'];

const EventConstants = {
  FilterableFields,
  SearchableFields,
};

export default EventConstants;
